(()=>{
'use strict';
const DB=()=>window.InventoryDB;const uid=()=>crypto.randomUUID?crypto.randomUUID():Date.now()+'_'+Math.random().toString(16).slice(2);const now=()=>new Date().toISOString();const text=v=>String(v??'').trim();const jan=v=>String(v??'').replace(/\D/g,'');async function all(name){return DB().getAll(name);}
async function productStock(id){return (await all('lots')).filter(l=>l.productId===id).reduce((s,l)=>s+Number(l.qty||0),0);}
async function saveProduct(input){const name=text(input.name);if(!name)throw new Error('商品名は必須です');const code=jan(input.jan);if(code&&![8,12,13].includes(code.length))throw new Error('JANコードは8・12・13桁で指定してください');const products=await all('products');if(code&&products.some(p=>p.id!==input.id&&p.active!==false&&jan(p.jan)===code))throw new Error('同じJANコードの商品が既にあります');const old=input.id?await DB().get('products',input.id):null;const locationId=input.locationId||null;if(locationId){const loc=await DB().get('locations',locationId);if(!loc)throw new Error('保管場所が見つかりません');if(loc.active===false)throw new Error('使用停止中の保管場所は指定できません');if(loc.parentId){const parent=await DB().get('locations',loc.parentId);if(parent?.active===false)throw new Error('親の保管場所が使用停止中です');}}if(old&&old.locationId!==locationId&&(await productStock(old.id))>0)throw new Error('在庫が残っている商品は保管場所を変更できません');const supplierId=input.supplierId||null;let supplier='';if(supplierId){const s=await DB().get('suppliers',supplierId);if(!s)throw new Error('仕入先が見つかりません');if(s.active===false)throw new Error('使用停止中の仕入先は指定できません');supplier=s.name;}const categoryId=input.categoryId||null;if(categoryId&&!await DB().get('categories',categoryId))throw new Error('分類が見つかりません');const product={...old,id:input.id||uid(),name,spec:text(input.spec),jan:code,supplierId,supplier,locationId,categoryId,casePack:Math.max(1,Math.floor(Number(input.casePack)||1)),active:old?.active!==false,createdAt:old?.createdAt||now(),updatedAt:now()};await DB().put('products',product);return product;}
async function setProductActive(id,active){const p=await DB().get('products',id);if(!p)throw new Error('商品が見つかりません');if(!active&&(await productStock(id))>0)throw new Error(`在庫が残っているため使用停止できません（${await productStock(id)}個）`);const next={...p,active:!!active,updatedAt:now()};await DB().put('products',next);let warning=null;if(active&&p.locationId){const loc=await DB().get('locations',p.locationId);if(!loc)warning='設定されている保管場所が見つかりません';else if(loc.active===false)warning='設定されている保管場所は使用停止中です';else if(loc.parentId){const parent=await DB().get('locations',loc.parentId);if(parent?.active===false)warning='親の保管場所が使用停止中です';}}return{product:next,warning};}
async function saveLocation(input){const name=text(input.name);if(!name)throw new Error('保管場所名は必須です');const old=input.id?await DB().get('locations',input.id):null;const parentId=input.parentId||null;if(input.id&&parentId===input.id)throw new Error('保管場所を自分自身の子にはできません');if(parentId){const parent=await DB().get('locations',parentId);if(!parent)throw new Error('親の保管場所が見つかりません');if(parent.parentId)throw new Error('保管場所は2階層までです');if(parent.active===false)throw new Error('使用停止中の場所には棚を追加できません');}if(old&&old.parentId!==parentId){const locations=await all('locations');if(locations.some(x=>x.parentId===old.id))throw new Error('子の棚・区画がある場所は階層を変更できません');const products=await all('products');if(products.some(p=>p.locationId===old.id))throw new Error('商品が登録されている場所は階層を変更できません');}const num=input.displayNumber==null||input.displayNumber===''?null:Number(input.displayNumber);if(num!==null&&(!Number.isFinite(num)||num<0))throw new Error('表示番号が不正です');const loc={...old,id:input.id||uid(),name,parentId,displayNumber:num,order:Number.isFinite(Number(input.order))?Number(input.order):0,active:old?.active!==false,createdAt:old?.createdAt||now(),updatedAt:now()};await DB().put('locations',loc);return loc;}
async function setLocationActive(id,active){const loc=await DB().get('locations',id);if(!loc)throw new Error('保管場所が見つかりません');if(!active){const [locations,products,lots]=await Promise.all([all('locations'),all('products'),all('lots')]);if(locations.some(x=>x.parentId===id&&x.active!==false))throw new Error('使用中の棚・区画があるため停止できません');const here=products.filter(p=>p.locationId===id);if(here.some(p=>p.active!==false))throw new Error('使用中の商品が登録されているため停止できません');const ids=new Set(here.map(p=>p.id));if(lots.some(l=>ids.has(l.productId)&&Number(l.qty||0)>0))throw new Error('在庫が残っているため停止できません');}const next={...loc,active:!!active,updatedAt:now()};await DB().put('locations',next);return next;}
async function saveSupplier(input){const name=text(input.name);if(!name)throw new Error('仕入先名は必須です');const suppliers=await all('suppliers');if(suppliers.some(s=>s.id!==input.id&&s.active!==false&&s.name.normalize('NFKC').toLowerCase()===name.normalize('NFKC').toLowerCase()))throw new Error('同じ仕入先名が既にあります');const old=input.id?await DB().get('suppliers',input.id):null;const item={...old,id:input.id||uid(),name,active:old?.active!==false,createdAt:old?.createdAt||now(),updatedAt:now()};await DB().put('suppliers',item);return item;}
async function setSupplierActive(id,active){const item=await DB().get('suppliers',id);if(!item)throw new Error('仕入先が見つかりません');const next={...item,active:!!active,updatedAt:now()};await DB().put('suppliers',next);return next;}
async function saveCategory(input){const name=text(input.name);if(!name)throw new Error('分類名は必須です');const old=input.id?await DB().get('categories',input.id):null;const parentId=input.parentId||null;if(input.id&&parentId===input.id)throw new Error('分類を自分自身の子にはできません');if(parentId){const cats=await all('categories');const parent=cats.find(c=>c.id===parentId);if(!parent)throw new Error('親分類が見つかりません');let depth=1,cur=parent;const seen=new Set([input.id].filter(Boolean));while(cur?.parentId){if(seen.has(cur.id))throw new Error('分類の循環参照はできません');seen.add(cur.id);cur=cats.find(c=>c.id===cur.parentId);if(!cur)throw new Error('分類階層が壊れています');depth++;if(depth>=3)throw new Error('分類は3階層までです');}}const item={...old,id:input.id||uid(),name,parentId,active:old?.active!==false,createdAt:old?.createdAt||now(),updatedAt:now()};await DB().put('categories',item);return item;}
window.MasterService={saveProduct,setProductActive,saveLocation,setLocationActive,saveSupplier,setSupplierActive,saveCategory};
})();
