(()=>{
'use strict';
const $=id=>document.getElementById(id);const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function showManage(){document.querySelectorAll('.view').forEach(v=>v.classList.toggle('hidden',v.id!=='manageView'));window.scrollTo(0,0);}
function message(text){const e=$('toast');e.textContent=text;e.classList.add('show');clearTimeout(message.t);message.t=setTimeout(()=>e.classList.remove('show'),2400);}
async function render(){const[products,locations,lots]=await Promise.all([InventoryDB.getAll('products'),InventoryDB.getAll('locations'),InventoryDB.getAll('lots')]);const stock=id=>lots.filter(l=>l.productId===id).reduce((s,l)=>s+Number(l.qty||0),0);$('manageProductList').innerHTML=products.length?'':'<div class="empty">商品はありません。</div>';for(const p of products.sort((a,b)=>String(a.name).localeCompare(String(b.name),'ja'))){const loc=locations.find(l=>l.id===p.locationId),qty=stock(p.id),active=p.active!==false,e=document.createElement('div');e.className='list-item manage-item';e.innerHTML=`<div><strong>${esc(p.name)}</strong><small>${esc(loc?.name||'保管場所未設定')} / 在庫 ${qty}個 / ${active?'使用中':'使用停止中'}</small></div><button class="small-btn ${active?'danger-btn':'secondary'}">${active?'使用停止':'再開'}</button>`;e.querySelector('button').onclick=async()=>{try{const result=await MasterService.setProductActive(p.id,!active);message(!active?'商品を再開しました':'商品を使用停止しました');if(result.warning)message('再開しました。注意: '+result.warning);await render();}catch(err){message(err.message);}};$('manageProductList').appendChild(e);}
$('manageLocationList').innerHTML=locations.length?'':'<div class="empty">保管場所はありません。</div>';const ordered=[...locations].sort((a,b)=>{const ap=a.parentId?1:0,bp=b.parentId?1:0;return ap-bp||(a.displayNumber??Infinity)-(b.displayNumber??Infinity)||String(a.name).localeCompare(String(b.name),'ja');});for(const l of ordered){const parent=locations.find(x=>x.id===l.parentId),active=l.active!==false,e=document.createElement('div');e.className='list-item manage-item';e.innerHTML=`<div><strong>${esc(parent?parent.name+' ＞ '+l.name:l.name)}</strong><small>${active?'使用中':'使用停止中'}</small></div><button class="small-btn ${active?'danger-btn':'secondary'}">${active?'使用停止':'再開'}</button>`;e.querySelector('button').onclick=async()=>{try{await MasterService.setLocationActive(l.id,!active);message(!active?'保管場所を再開しました':'保管場所を使用停止しました');await render();}catch(err){message(err.message);}};$('manageLocationList').appendChild(e);}}
$('manageBtn').addEventListener('click',async()=>{try{await render();showManage();}catch(err){message(err.message);}});
})();