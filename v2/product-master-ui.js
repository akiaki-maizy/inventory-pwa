(()=>{
'use strict';
const $=id=>document.getElementById(id);let editingProductId=null;const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
function msg(text){const e=$('toast');e.textContent=text;e.classList.add('show');clearTimeout(msg.t);msg.t=setTimeout(()=>e.classList.remove('show'),2400);}
function show(id){document.querySelectorAll('.view').forEach(v=>v.classList.toggle('hidden',v.id!==id));window.scrollTo(0,0);}
async function productOptions(selected={}){const[locations,suppliers,cats]=await Promise.all([InventoryDB.getAll('locations'),InventoryDB.getAll('suppliers'),InventoryDB.getAll('categories')]);const activeLoc=locations.filter(x=>x.active!==false||x.id===selected.locationId);$('productLocation').innerHTML='<option value="">選択してください</option>'+activeLoc.map(x=>{const p=locations.find(y=>y.id===x.parentId);return `<option value="${x.id}">${esc(p?p.name+' ＞ '+x.name:x.name)}${x.active===false?'（停止中）':''}</option>`;}).join('');$('productSupplier').innerHTML='<option value="">未設定</option>'+suppliers.filter(x=>x.active!==false||x.id===selected.supplierId).sort((a,b)=>a.name.localeCompare(b.name,'ja')).map(x=>`<option value="${x.id}">${esc(x.name)}${x.active===false?'（停止中）':''}</option>`).join('');function path(c){const names=[c.name];let cur=c;for(let i=0;i<2&&cur.parentId;i++){cur=cats.find(x=>x.id===cur.parentId);if(cur)names.unshift(cur.name);}return names.join(' ＞ ');}$('productCategory').innerHTML='<option value="">未設定</option>'+cats.filter(x=>x.active!==false||x.id===selected.categoryId).sort((a,b)=>path(a).localeCompare(path(b),'ja')).map(x=>`<option value="${x.id}">${esc(path(x))}${x.active===false?'（停止中）':''}</option>`).join('');}
async function openProduct(id=null){editingProductId=id;const p=id?await InventoryDB.get('products',id):null;await productOptions(p||{});$('productFormTitle').textContent=p?'商品の編集':'商品の登録';$('productName').value=p?.name||'';$('productSpec').value=p?.spec||'';$('productJan').value=p?.jan||'';$('productSupplier').value=p?.supplierId||'';$('productCategory').value=p?.categoryId||'';$('productLocation').value=p?.locationId||'';$('productCasePack').value=p?.casePack||1;show('productView');}
async function saveProduct(){try{const old=editingProductId?await InventoryDB.get('products',editingProductId):null;await MasterService.saveProduct({id:editingProductId||undefined,name:$('productName').value,spec:$('productSpec').value,jan:$('productJan').value,supplierId:$('productSupplier').value||null,categoryId:$('productCategory').value||null,locationId:$('productLocation').value||null,casePack:$('productCasePack').value});editingProductId=null;msg(old?'商品を更新しました':'商品を保存しました');location.reload();}catch(e){msg(e.message);}}
window.ProductMasterUI={openProduct};
$('addProductBtn').onclick=()=>openProduct();$('saveProductBtn').onclick=saveProduct;
})();