# Inventory PWA Ver.1.13 安定化監査

基準版: MVP Ver.1.13
方針: 機能破損を避け、既存データ互換性を最優先する。大規模な一括リファクタリングは行わない。

## 現在の主要構成

- `index.html`: 既存UI、IndexedDB、基本在庫機能の本体
- `jan-scanner.js`: JAN読取、仕入先マスタ、ホーム機能整理などの拡張
- `case-stock.js`: ケース/バラ、FEFO使用、複数期限登録などの拡張
- `date-wheel.js`: 賞味期限ロール入力
- `service-worker.js`: PWAキャッシュ、起動時の安定化

## 監査で確認した技術的負債

1. `index.html` に旧ホームUIが残り、起動後に `jan-scanner.js` が現在UIへ変更している。
2. バージョン表示処理が複数ファイルに分散していた。
3. `case-stock.js` は既存のグローバル関数やボタン処理へ後付けで処理を差し込む箇所があり、今後の変更時に回帰リスクがある。
4. 仕入先マスタは `localStorage`、在庫本体は IndexedDB であり、バックアップ対象の統合確認が必要。
5. Service Worker がHTMLを起動時に補正する処理を持つ。最終的には現在UIを `index.html` の正式構造へ移し、補正処理を減らすのが望ましい。

## Ver.1.13 安定化で実施済み

- 旧バージョン表示の初回ちらつきを抑止
- 旧ホーム7ボタンの初回ちらつきを抑止
- `date-wheel.js` から重複したバージョン表示書き換えを削除
- 賞味期限ロールの保存値は従来どおり `YYYY-MM-DD` を維持
- IndexedDB の既存スキーマは変更しない

## 変更禁止領域（回帰確認なしでは変更しない）

- 商品ID・ロットIDの生成方式
- IndexedDB のストア名・既存データ形式
- ロット数量 `qty` の基準単位
- FEFOの期限順消費ロジック
- JSONバックアップ/復元形式
- CSV出力形式

## 回帰テストチェックリスト

- [ ] ホームがカテゴリ式で表示され、旧7ボタンが一瞬表示されない
- [ ] 商品を新規登録できる
- [ ] 仕入先を選択できる
- [ ] ケース入数を登録できる
- [ ] 商品登録時に複数賞味期限を登録できる
- [ ] 通常入荷ができる
- [ ] 複数期限まとめて入荷ができる
- [ ] 賞味期限ロールが今日を初期値として表示される
- [ ] ケース/バラ表示が正しい
- [ ] 使用登録で期限の近いロットから減る
- [ ] 廃棄登録ができる
- [ ] 棚卸ができる
- [ ] JANカメラ読取ができる
- [ ] JAN手入力へフォールバックできる
- [ ] 履歴を確認できる
- [ ] JSONバックアップを書き出せる
- [ ] JSONバックアップを復元できる
- [ ] CSVを書き出せる
- [ ] ブラウザ更新後も既存在庫が残る

## 次の整理順

1. バージョン表示の完全一元化
2. 現在のカテゴリ式ホームを `index.html` の正式UIへ移行
3. Service Worker のHTML補正処理を縮小
4. 仕入先マスタをバックアップ/復元対象へ統合
5. `case-stock.js` の後付け処理を機能単位で段階整理

各段階で上記回帰テストを行い、問題があれば直前の安定状態へ戻せる単位でコミットする。
